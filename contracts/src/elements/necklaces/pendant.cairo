// Internal imports

use grimscape::elements::necklaces::interface::{NecklaceTrait, Tier};

// Implementations

impl Pendant of NecklaceTrait {
    #[inline]
    fn tier() -> Tier {
        return Tier::S;
    }
}
